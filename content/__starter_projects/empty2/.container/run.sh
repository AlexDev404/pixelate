# Essentially an empty project, just keep the container running
# You can replace this with your own run instructions if needed
# For now, it will just keep the container alive.
apk add python3
tail -f /dev/null