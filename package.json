{
  "name": "pixelate",
  "private": true,
  "workspaces": [
    "types",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w types && npm run build -w backend && npm run build -w frontend",
    "typecheck": "npm run typecheck -w types && npm run typecheck -w backend && npm run typecheck -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
